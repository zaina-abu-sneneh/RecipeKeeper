name: recipe_keeper
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

# pub.dev for dependencies
dependencies:
  cloud_firestore: ^6.1.0
  firebase_auth: ^6.1.2
  firebase_core: ^4.2.1
  flutter:
    sdk: flutter
  google_fonts: ^6.3.2
  lottie: ^3.3.2
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets: 
  - assets/images/
  - assets/animations/
